services:
  s3admin:
    image: dpage/pgadmin4:latest
    container_name: 68_s3_ILoveMyJob_admin
    restart: unless-stopped
    env_file:
      - .env
    environment:
      PGADMIN_DEFAULT_EMAIL: "${PGADMIN_DEFAULT_EMAIL}"
      PGADMIN_DEFAULT_PASSWORD: "${PGADMIN_DEFAULT_PASSWORD}"
    ports:
      - "${ADMIN_PORT}:80"
    depends_on:
      - s3db
