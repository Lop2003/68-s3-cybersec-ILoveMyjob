services:
  s3web:
    image: caddy:latest
    container_name: 68_s3_ILoveMyJob_web
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "${WEB_PORT}:443"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile
    depends_on:
      - s3app
